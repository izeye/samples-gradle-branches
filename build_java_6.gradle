apply plugin: 'java'

sourceCompatibility=1.6
targetCompatibility=1.6

compileJava {
    source = source.matching {
        include 'com/izeye/sample/gradle/domain/**',
                'com/izeye/sample/gradle/service/**'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}